//
// Entry point for Lambda to invoke. This gets dynamically added to each Lambda zipfile.
//

var handler = function(event, context) {
  context.done(null, 'Not implemented yet.');
};

exports.handler = handler;
